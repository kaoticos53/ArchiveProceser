<UserControl x:Class="FileFlow.App.Views.Components.EditorZoomBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:FileFlow.App.ViewModels"
             xmlns:loc="clr-namespace:FileFlow.Sdk.Localization;assembly=FileFlow.Sdk"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:EditorViewModel}">
    <Border Background="{DynamicResource BgCardBrush}"
            BorderBrush="{DynamicResource BorderDarkBrush}"
            BorderThickness="1"
            CornerRadius="8"
            Padding="8,4"
            HorizontalAlignment="Right"
            VerticalAlignment="Bottom"
            Margin="0,0,16,16">
        <Border.Effect>
            <DropShadowEffect BlurRadius="12" ShadowDepth="2" Direction="270" Color="#000000" Opacity="0.25" />
        </Border.Effect>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <!-- Zoom Out Button -->
            <Button Command="{Binding ZoomOutCommand}" Style="{DynamicResource SecondaryButton}" Padding="6,2" Width="28" Height="24" 
                    ToolTip="{Binding Source={x:Static loc:LocalizationManager.Instance}, Path=[Zoom_OutToolTip]}">
                <TextBlock Text="−" FontWeight="Bold" FontSize="14" Foreground="{DynamicResource TextPrimaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-2,0,0"/>
            </Button>
            
            <!-- Zoom Slider -->
            <Slider Width="90" Minimum="0.2" Maximum="2.0" Value="{Binding ViewportZoom, Mode=TwoWay}"
                    SmallChange="0.05" LargeChange="0.05" VerticalAlignment="Center" Margin="8,0" />
            
            <!-- Zoom In Button -->
            <Button Command="{Binding ZoomInCommand}" Style="{DynamicResource SecondaryButton}" Padding="6,2" Width="28" Height="24" 
                    ToolTip="{Binding Source={x:Static loc:LocalizationManager.Instance}, Path=[Zoom_InToolTip]}">
                <TextBlock Text="+" FontWeight="Bold" FontSize="14" Foreground="{DynamicResource TextPrimaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-1,0,0"/>
            </Button>
            
            <!-- Zoom Percentage Label -->
            <TextBlock Text="{Binding ViewportZoom, StringFormat={}{0:P0}}" 
                       FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" 
                       VerticalAlignment="Center" Width="40" TextAlignment="Center" Margin="4,0,2,0"/>
            
            <!-- Fit To Screen Button -->
            <Button Command="{Binding FitToScreenCommand}" Style="{DynamicResource SecondaryButton}" Padding="6,2" Height="24" 
                    ToolTip="{Binding Source={x:Static loc:LocalizationManager.Instance}, Path=[Zoom_FitToolTip]}" Margin="2,0,4,0">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="⛶" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource AccentGlowBrush}" Margin="0,0,4,0" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding Source={x:Static loc:LocalizationManager.Instance}, Path=[Zoom_FitBtn]}" FontSize="10" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>

            <!-- Add Note Button -->
            <Button Command="{Binding AddNewAnnotationCommand}" Style="{DynamicResource SecondaryButton}" Padding="6,2" Height="24" 
                    ToolTip="{Binding Source={x:Static loc:LocalizationManager.Instance}, Path=[Zoom_AddNoteToolTip]}" Margin="2,0,4,0">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="📝" FontSize="11" Margin="0,0,4,0" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding Source={x:Static loc:LocalizationManager.Instance}, Path=[Zoom_NoteBtn]}" FontSize="10" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>

            <!-- Add Group Button -->
            <Button Command="{Binding GroupSelectedNodesCommand}" Style="{DynamicResource SecondaryButton}" Padding="6,2" Height="24" 
                    ToolTip="{Binding Source={x:Static loc:LocalizationManager.Instance}, Path=[Zoom_GroupToolTip]}" Margin="2,0,0,0">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🔲" FontSize="11" Margin="0,0,4,0" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding Source={x:Static loc:LocalizationManager.Instance}, Path=[Zoom_GroupBtn]}" FontSize="10" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </StackPanel>
    </Border>
</UserControl>
