<UserControl x:Class="FileFlow.App.Views.NodeToolboxView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:FileFlow.App.ViewModels"
             xmlns:loc="clr-namespace:FileFlow.Sdk.Localization;assembly=FileFlow.Sdk"
             xmlns:conv="clr-namespace:FileFlow.App.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="280">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <conv:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter" />
    </UserControl.Resources>

    <Border Background="{DynamicResource BgCardBrush}" 
            BorderBrush="{DynamicResource BorderDarkBrush}" 
            BorderThickness="0,0,1,0" 
            Padding="12,10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Header Title & View Mode Toggle -->
            <Grid Grid.Row="0" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" 
                           Text="{Binding Source={x:Static loc:LocalizationManager.Instance}, Path=[NodeToolboxTitle]}" 
                           FontSize="13.5" 
                           FontWeight="Bold" 
                           Foreground="{DynamicResource TextPrimaryBrush}" 
                           VerticalAlignment="Center" />

                <!-- Toggle Compact / Detailed View Mode -->
                <ToggleButton Grid.Column="1"
                              IsChecked="{Binding IsCompactMode, Mode=TwoWay}"
                              Style="{StaticResource SecondaryToggleButton}"
                              Padding="6,2" FontSize="10.5"
                              Cursor="Hand"
                              ToolTip="{Binding Source={x:Static loc:LocalizationManager.Instance}, Path=[Toolbox_ToggleCompactToolTip]}">
                    <ToggleButton.Content>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="☰ " FontSize="10" VerticalAlignment="Center" />
                            <TextBlock Text="{Binding Source={x:Static loc:LocalizationManager.Instance}, Path=[Toolbox_CompactBtn]}" FontSize="10.5" VerticalAlignment="Center" />
                        </StackPanel>
                    </ToggleButton.Content>
                </ToggleButton>
            </Grid>

            <!-- Search Input with Fluent Icon -->
            <Grid Grid.Row="1" Margin="0,0,0,8">
                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                         Background="{DynamicResource BgSurfaceBrush}" 
                         Foreground="{DynamicResource TextPrimaryBrush}" 
                         BorderBrush="{DynamicResource BorderDarkBrush}" 
                         Padding="26,5,8,5" 
                         FontSize="11.5" />
                <TextBlock Text="🔍" 
                           FontSize="11" 
                           Foreground="{DynamicResource TextSecondaryBrush}" 
                           HorizontalAlignment="Left" 
                           VerticalAlignment="Center" 
                           Margin="8,0,0,0" 
                           IsHitTestVisible="False" />
            </Grid>

            <!-- Modern Category Dropdown Selector (Compact 1-Row) -->
            <Grid Grid.Row="2" Margin="0,0,0,10">
                <ComboBox ItemsSource="{Binding AvailableCategories}"
                          SelectedItem="{Binding SelectedCategoryItem, Mode=TwoWay}"
                          Background="{DynamicResource BgSurfaceBrush}"
                          Foreground="{DynamicResource TextPrimaryBrush}"
                          BorderBrush="{DynamicResource BorderDarkBrush}"
                          Padding="8,5"
                          FontSize="11.5"
                          Cursor="Hand">
                    <ComboBox.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:ToolboxCategoryFilterItem}">
                            <Grid Margin="2,1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding Icon}" FontSize="12" Margin="0,0,8,0" VerticalAlignment="Center" />
                                <TextBlock Grid.Column="1" Text="{Binding DisplayName}" FontWeight="SemiBold" FontSize="11" Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center" />
                                <Border Grid.Column="2" Background="#20808080" CornerRadius="8" Padding="6,1" Margin="8,0,0,0" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Count}" FontSize="9.5" FontWeight="Bold" Foreground="{DynamicResource TextSecondaryBrush}" />
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </Grid>

            <!-- Categorized Nodes List -->
            <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding CategoryGroups}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Expander IsExpanded="True" Margin="0,0,0,8">
                                <Expander.Header>
                                    <TextBlock Text="{Binding CategoryName}" 
                                               Foreground="{DynamicResource AccentGlowBrush}" 
                                               FontWeight="Bold" 
                                               FontSize="12" 
                                               VerticalAlignment="Center" />
                                </Expander.Header>
                                <ItemsControl ItemsSource="{Binding Items}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="{DynamicResource BgSurfaceBrush}" 
                                                    BorderBrush="{DynamicResource BorderDarkBrush}" 
                                                    BorderThickness="1" 
                                                    CornerRadius="6" 
                                                    Padding="8,5" 
                                                    Margin="0,2" 
                                                    Cursor="Hand"
                                                    MouseLeftButtonDown="NodeItem_MouseLeftButtonDown">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Style.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="{DynamicResource BgHoverBrush}" />
                                                                <Setter Property="BorderBrush" Value="{DynamicResource AccentPrimaryBrush}" />
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>

                                                <!-- ToolTip Rico con Descripción -->
                                                <Border.ToolTip>
                                                    <ToolTip Background="Transparent" BorderThickness="0" Padding="0">
                                                        <Border Background="{DynamicResource BgCardBrush}" 
                                                                BorderBrush="{DynamicResource BorderDarkBrush}" 
                                                                BorderThickness="1.5" CornerRadius="8" Padding="12,10" Width="260">
                                                            <Border.Effect>
                                                                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Color="#000000" Opacity="0.45" />
                                                            </Border.Effect>
                                                            <StackPanel>
                                                                <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                                                    <TextBlock Text="{Binding Icon}" FontSize="13" Margin="0,0,6,0" />
                                                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="12" Foreground="{DynamicResource TextPrimaryBrush}" />
                                                                </StackPanel>
                                                                <Separator Background="{DynamicResource BorderDarkBrush}" Margin="0,0,0,6" />
                                                                <TextBlock Text="{Binding Description}" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap" LineHeight="14" />
                                                            </StackPanel>
                                                        </Border>
                                                    </ToolTip>
                                                </Border.ToolTip>

                                                <StackPanel>
                                                    <!-- Fila de Icono, Título del Nodo y Botón Favorito -->
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="Auto" />
                                                            <ColumnDefinition Width="*" />
                                                            <ColumnDefinition Width="Auto" />
                                                        </Grid.ColumnDefinitions>

                                                        <TextBlock Grid.Column="0" Text="{Binding Icon}" FontSize="13" Margin="0,0,6,0" VerticalAlignment="Center" />
                                                        <TextBlock Grid.Column="1" Text="{Binding Name}" 
                                                                   FontWeight="SemiBold" 
                                                                   Foreground="{DynamicResource TextPrimaryBrush}" 
                                                                   FontSize="11.5" 
                                                                   TextTrimming="CharacterEllipsis"
                                                                   VerticalAlignment="Center" />

                                                        <!-- Star Button for Favorites -->
                                                        <Button Grid.Column="2"
                                                                Command="{Binding DataContext.ToggleFavoriteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                CommandParameter="{Binding}"
                                                                Background="Transparent"
                                                                BorderThickness="0"
                                                                Padding="4,0"
                                                                Margin="4,0,0,0"
                                                                Cursor="Hand"
                                                                ToolTip="{Binding Source={x:Static loc:LocalizationManager.Instance}, Path=[Toolbox_FavoriteToolTip]}">
                                                            <Button.Style>
                                                                <Style TargetType="Button">
                                                                    <Setter Property="Template">
                                                                        <Setter.Value>
                                                                            <ControlTemplate TargetType="Button">
                                                                                <TextBlock x:Name="StarIcon" Text="☆" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center" />
                                                                                <ControlTemplate.Triggers>
                                                                                    <DataTrigger Binding="{Binding IsFavorite}" Value="True">
                                                                                        <Setter TargetName="StarIcon" Property="Text" Value="⭐" />
                                                                                        <Setter TargetName="StarIcon" Property="Foreground" Value="#F59E0B" />
                                                                                    </DataTrigger>
                                                                                    <Trigger Property="IsMouseOver" Value="True">
                                                                                        <Setter TargetName="StarIcon" Property="Opacity" Value="0.8" />
                                                                                    </Trigger>
                                                                                </ControlTemplate.Triggers>
                                                                            </ControlTemplate>
                                                                        </Setter.Value>
                                                                    </Setter>
                                                                </Style>
                                                            </Button.Style>
                                                        </Button>
                                                    </Grid>

                                                    <!-- Descripción (Solo visible cuando NO se usa modo compacto) -->
                                                    <TextBlock Text="{Binding Description}" 
                                                               Foreground="{DynamicResource TextSecondaryBrush}" 
                                                               FontSize="10.5" 
                                                               TextWrapping="Wrap" 
                                                               Margin="0,4,0,0" 
                                                               LineHeight="13"
                                                               Visibility="{Binding DataContext.IsCompactMode, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
                                                </StackPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </Expander>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>
