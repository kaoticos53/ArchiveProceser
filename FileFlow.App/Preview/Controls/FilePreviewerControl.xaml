<UserControl x:Class="FileFlow.App.Preview.Controls.FilePreviewerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"
             Background="#0B0D14">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="280" x:Name="MetadataPanelColumn" />
        </Grid.ColumnDefinitions>

        <!-- Área Central de Visualización -->
        <Grid Grid.Column="0" Background="#111318">
            <ContentControl Content="{Binding ActiveVisualElement}" />

            <!-- Indicador de Carga -->
            <Border Background="#99000000" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                    HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ProgressBar IsIndeterminate="True" Width="180" Height="4" Margin="0,0,0,12" />
                    <TextBlock Text="{Binding StatusMessage}" Foreground="White" FontSize="13" HorizontalAlignment="Center" />
                </StackPanel>
            </Border>
        </Grid>

        <!-- Divisor de Columna -->
        <GridSplitter Grid.Column="1" Width="3" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="#1F232D" />

        <!-- Panel Lateral de Metadatos y Trazabilidad -->
        <Border Grid.Column="2" Background="#141720" BorderBrush="#202533" BorderThickness="1,0,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Cabecera del Panel Lateral -->
                <Border Grid.Row="0" Background="#1A1D27" Padding="14,10" BorderBrush="#252A3A" BorderThickness="0,0,0,1">
                    <TextBlock Text="🏷️ Ficha de Metadatos" Foreground="#00E5FF" FontWeight="Bold" FontSize="13" />
                </Border>

                <!-- Lista de Metadatos -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="10">
                    <ItemsControl ItemsSource="{Binding MetadataItems}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#1A1E29" BorderBrush="#2A3042" BorderThickness="1" CornerRadius="6" Margin="0,0,0,6" Padding="8,6">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Key}" Foreground="#8F95A3" FontSize="11" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                                        <TextBlock Text="{Binding Value}" Foreground="#E1E4EA" FontSize="12" Margin="0,2,0,0" TextWrapping="Wrap" />
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <!-- Botones de Acción Rápida -->
                <Border Grid.Row="2" Background="#1A1D27" Padding="10" BorderBrush="#252A3A" BorderThickness="0,1,0,0">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="📋 Copiar Ruta" Command="{Binding CopyPathToClipboardCommand}" Padding="10,6" Margin="0,0,8,0" Cursor="Hand" />
                        <Button Content="📂 Explorador" Command="{Binding OpenInExplorerCommand}" Padding="10,6" Background="#00D2FF" Foreground="#0B0D14" FontWeight="Bold" Cursor="Hand" />
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
